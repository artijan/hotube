extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="기존 비밀번호" name="oldPassword" type="password")
        input(placeholder="새로운 비밀번호" name="newPassword" type="password")
        input(placeholder="비밀번호 확인" name="newPaswwordConfirm" type="password")
        input(value="확인" type="submit")
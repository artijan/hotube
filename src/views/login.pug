extends base


block content   
    if errorMessage     
        span=errorMessage
    
    form(method="POST")
        input(placeholder="닉네임", name="username" , type="text", required)
        input(placeholder="비밀번호", name="password" , type="password", required)
        input(type="submit", value="로그인")
        div.social-login
            a(href="/users/github/start").social__btn.social__btn-github
                i.fab.fa-github
                | 깃허브로 로그인 &rarr;
    div.auth-switch
        span 계정이 없으신가요?
        a(href="/join") 가입하러 가기 &rarr;
